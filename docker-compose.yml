services:
  backend:
    build: ./backend
    ports:
      - "3001:3001"
    volumes:
      - rsu-data:/data
    environment:
      DATABASE_URL: /data/rsu.db

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend

volumes:
  rsu-data:
